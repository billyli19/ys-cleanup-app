// Define an interface for user login data.
export interface IUserLogin {
    email: string;     // Represents the user's email address for login.
    password: string;  // Represents the user's password for login.
}
