// Define the base URL for API requests.
const BASE_URL = 'http://localhost:8080';

// Define constant URLs for user authentication and registration.
export const USER_LOGIN_URL = BASE_URL + '/api/users/signin'; // URL for user login.
export const USER_REGISTER_URL = BASE_URL + '/api/users/register'; // URL for user registration.
